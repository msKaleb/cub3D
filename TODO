-quitar -Wno-deprecated de makefile de minilibx
-controlar relink en bonus
-Quitar todos los "//DELETE"
-Check si los pasos de texturas son valiabres (existe, tiene perimso?)
-Buscar todo malloc, calloc, strdup, etc. por todo el código
-Limitar el la velocidad de movimiento al framerate
-modificar print_line() para pasar el color concreto de la textura(ahora usa colores por defecto)
-mlx_new_image() para almacenar el mapa y después mlx_put_image_to_window()