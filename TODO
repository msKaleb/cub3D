quitar -Wno-deprecated de makefile de minilibx
controlar relink en bonus
Quitar todos los "//DELETE"
en count_cols(), tal vez haya que cambiar (*data)->pt_person.x/y por player->pos_x/y
mi compilador dice lo siguiente en parser.c (linea "if (&line[i] && !ft_strncmp(&line[i], "NO ", 3))")
	"parser.c:187:22: error: the comparison will always evaluate as 'true' for the pointer operand in 'line + (sizetype)i'"
	quizá sea mejor poner al principio de la función "if (!line) return;" y quitar "&line[i]" de cada if